name: "Build package action"
on:
  pull_request:
  push:
    branches:
      - mpris

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@mpris
    - run: | 
          sudo npm install -g electron
          sudo npm install -g asar
    - run: ./.github/PKGBUILD
 
