name: "Build package action"
on:
  pull_request:
  push:
    branches:
      - mpris

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: | 
          sudo apt-get install electron
          sudo npm install -g asar
          ./PKGBUILD
    - uses: ./
